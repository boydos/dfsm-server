<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ds.dfsm.dao.UserDao">

    <resultMap id="userMap" type="user">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <!--<result column="name" property="name" jdbcType="CHAR"/>
        <result column="nickname" property="nickname" jdbcType="CHAR"/>
        <result column="password" property="password" jdbcType="CHAR"/>
        <result column="role" property="role" jdbcType="INTEGER"/>-->
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP"/>
        <result column="gmt_modify" property="gmtModify" jdbcType="TIMESTAMP"/>
        <result column="is_deleted" property="status" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="tableName">
        dfsm_user
    </sql>
    <sql id="columns">
        id,name,nickname,role,gmt_create,gmt_modify,is_deleted
    </sql>

    <select id="listUser" resultMap="userMap">
        select <include refid="columns"/>
        from <include refid="tableName"/>
    </select>

    <select id="getUser" parameterType="Long" resultMap="userMap">
        select <include refid="columns"/>
        from <include refid="tableName"/>
        where id = #{id}
    </select>

    <insert id="insertUser" parameterType="user" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        insert into <include refid="tableName"/>
        (name, password, nickname, gmt_create, gmt_modify)
        values (#{name}, #{password}, #{nickname},#{gmtCreate, jdbcType=TIMESTAMP},#{gmtModify, jdbcType=TIMESTAMP})
    </insert>

    <update id="updateUser" parameterType="user">
        update <include refid="tableName"/>
        <trim prefix="set" suffixOverrides=",">
            <if test="name != null">
                name = #{name},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="nickname != null">
                nickname = #{nickname},
            </if>
            <if test="role != null">
                role = #{role},
            </if>
            <if test="status != null">
                is_deleted = #{status},
            </if>
            <if test="gmtModify != null">
                gmt_modify = #{gmtModify},
            </if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteUser" parameterType="Long">
        delete from <include refid="tableName"/>
        where id = #{id}
    </delete>
</mapper>